{% extends 'base.html.twig' %}

{% block title %}Product{% endblock %}

{% block body %}
    <h1>Product</h1>
    <div class="container">
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ product.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ product.name }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ product.price }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_crud_product_index') }}">back to list</a>

    <a href="{{ path('app_crud_product_edit', {'id': product.id}) }}">edit</a>

    {{ include('crud_product/_delete_form.html.twig') }}
    </div>
{% endblock %}
